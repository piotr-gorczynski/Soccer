substitutions:
  _ENVIRONMENT: 'dev'
  _FOLDER_NAME: 'soccer'

steps:
  # Step 1: Get Firebase Project ID
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk:slim'
    id: 'get-project-id'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        set -e
        echo "🔍 Looking for project starting with ${_FOLDER_NAME}-${_ENVIRONMENT}..."
        gcloud projects list \
          --filter="name~^${_FOLDER_NAME}-${_ENVIRONMENT}" \
          --format="value(projectId)" | head -n 1 > /workspace/project_id.txt

        if [ ! -s /workspace/project_id.txt ]; then
          echo "❌ Firebase project not found."
          exit 1
        fi

        echo "✅ Found project ID: $(cat /workspace/project_id.txt)"

  # Step 2: Enable Identity Platform API (required by Terraform resource)
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk:slim'
    id: 'enable-identityplatform-api'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        set -e
        firebase_project_id=$(cat /workspace/project_id.txt)
        echo "🔓 Enabling Identity Platform API for project: $firebase_project_id..."
        gcloud services enable identityplatform.googleapis.com --project="$firebase_project_id"
        echo "✅ identityplatform.googleapis.com enabled."

  # Step 3: Write Terraform config using google-beta provider
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk:slim'
    id: 'write-tf-config'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        set -e
        mkdir -p /workspace/tf-auth
        cat <<EOF > /workspace/tf-auth/main.tf
        terraform {
          required_providers {
            google-beta = {
              source  = "hashicorp/google-beta"
              version = ">= 4.80.0"
            }
          }
        }

        provider "google-beta" {
          project = "$(cat /workspace/project_id.txt)"
        }

        resource "google_identity_platform_project_default_config" "default" {
          provider = google-beta
          project  = "$(cat /workspace/project_id.txt)"
          sign_in {
            email {
              enabled = true
            }
            anonymous {
              enabled = false
            }
            phone_number {
              enabled = false
            }
          }
        }
        EOF

  # Step 4: Terraform Init
  - name: 'hashicorp/terraform:light'
    id: 'terraform-init'
    dir: '/workspace/tf-auth'
    entrypoint: 'sh'
    args: ['-c', 'terraform init']

  # Step 5: Terraform Apply
  - name: 'hashicorp/terraform:light'
    id: 'terraform-apply'
    dir: '/workspace/tf-auth'
    entrypoint: 'sh'
    args: ['-c', 'terraform apply -auto-approve']

options:
  logging: CLOUD_LOGGING_ONLY
