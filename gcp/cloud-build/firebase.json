{
    "firestore": {
      "rules": "service cloud.firestore { match /databases/{database}/documents { match /{document=**} { allow read, write: if false; } } }",
      "indexes": []
    },
    "database": {
      "rules": "{ \"rules\": { \".read\": \"auth != null\", \".write\": \"auth != null\" } }"
    },
    "functions": {
      "source": "functions"
    },
    "storage": {
      "rules": "rules_version = '2'; service firebase.storage { match /b/{bucket}/o { match /{allPaths=**} { allow read, write: if false; } } }"
    }
  }
  