<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required for Android 11+ package visibility: allows detecting if the global app is installed -->
    <queries>
        <package android:name="piotr_gorczynski.soccer2" />
    </queries>

    <application>
        <!-- Keep the authority unique so the Bangladesh and global apps can coexist. -->
        <provider
            android:name="com.facebook.FacebookContentProvider"
            android:authorities="com.facebook.app.FacebookContentProvider1232966491486195.bd"
            android:exported="true"
            tools:replace="android:authorities" />
    </application>

</manifest>
