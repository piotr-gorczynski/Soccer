<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorGreenDark</item>
        <item name="colorPrimaryDark">@color/colorGreenDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!-- 
            Important: windowBackground must NOT be transparent when using AppCompat themes.
            Transparent backgrounds cause "Window couldn't find content container view" error.
            Use colorWhite to match the activity_menu.xml background.
        -->
        <item name="android:windowBackground">@color/colorWhite</item>
     </style>

    <!-- Splash screen theme with loading background -->
    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorGreenDark</item>
        <item name="colorPrimaryDark">@color/colorGreenDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>

</resources>
