{
  "name": "force-start-stuck-matches",
  "description": "Scheduled function that runs every minute and starts any active match whose turnStartTime is still null after 60 s.",
  "version": "1.0.0",
  "main": "index.js",
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "firebase-admin": "^11.10.1",
    "firebase-functions": "^4.5.0"
  }
}
