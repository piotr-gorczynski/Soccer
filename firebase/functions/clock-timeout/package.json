{
  "name": "clock-timeout",
  "description": "Firestore onUpdate trigger that awards the game if the active player’s clock already elapsed.",
  "version": "1.0.0",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^11.10.1",
    "firebase-functions": "^4.5.0"
  }
}
